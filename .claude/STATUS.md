# .claude/STATUS.md

Phase-execution state and mid-run deviations. See `CLAUDE.md` "Autonomous Execution" for when rows land here.

## Unforeseen / checkpoints

| Date | Commit | What happened | What to do next |
|---|---|---|---|
| 2026-05-25 | 1A.2 | Doc-reviewer surfaced a Python import collision in the plan: 1D.1 plans `app/signals.py` (helper module) while 1D.8 plans `app/signals/<name>.py` package — Python cannot have both at the same path. **Decision**: rename `app/signals.py` → `app/signal_helpers.py`. The package `app/signals/` retains the per-signal rule-evaluation modules. | When executing 1D.1 (Day 4-5), create `app/signal_helpers.py` (not `app/signals.py`). When executing 1D.8, the `app/signals/` package coexists without collision. PLAN_PHASE_1.md text refers to `app/signals.py` in 1D.1; treat this STATUS row as authoritative for naming. |
| 2026-05-25 | post-1A.7 | Operator amendment: drop the `FG_` env-var prefix project-wide. Env vars match pydantic-settings field names verbatim (e.g. `DATABASE_URL` not `FG_DATABASE_URL`). 9 docs updated in a single follow-up commit; no Batch 1A commits rewritten. | When executing 1B.1, `.env.example` uses unprefixed names: `DATABASE_URL`, `HMAC_SECRET`, `API_TOKEN_PREFIX`, `MAXMIND_LICENSE_KEY`, `IP2PROXY_DOWNLOAD_TOKEN`, `LOG_LEVEL`, `AUTH_ENABLED`. `app/config.py` defines `Settings(BaseSettings)` with NO `env_prefix=` argument — pydantic-settings then reads `field_name.upper()` as the env var name. |
| 2026-05-25 | 1B.2 | Phase 1 RLS policies do not enforce because the app connects as the postgres bootstrap superuser (which bypasses RLS). The `tenant_isolation` policies are structurally complete on all 9 tenant-scoped tables and the `riskd_app` role exists (NOLOGIN, no BYPASSRLS) — but the connecting role is the superuser, so RLS is dormant until a non-superuser login role is introduced. | In Phase 5 hardening: create a non-superuser login role (e.g. `riskd_app_login`) with `LOGIN INHERIT`, `GRANT riskd_app TO riskd_app_login`, switch the runtime `DATABASE_URL` to connect as that role, and add an integration test that asserts a cross-tenant query under that role returns 0 rows. Until then, app-layer `tenant_id` filtering is the active defense; defense-in-depth via RLS activates on the role switch. |
| 2026-05-26 | 1B.3 | Reviewer-panel subagents (senior-engineer, security-auditor, code-flow) returned platform usage-quota errors mid-cycle — could not produce verdicts for 1B.3. Self-review performed against each reviewer's dimensions: ruff clean, mypy strict clean, import smoke test passes, no secrets in code, SQL parameterized, single-responsibility modules, plan-suppressed items consistent with plan. Committing 1B.3 with `Review: platform-quota-deferred` footer. | When quota resets (per operator: 2am Asia/Calcutta), invoke the full code-path panel against the combined diff of 1B.3 + 1B.4 (the smaller surface) OR run retrospectively against 1B.3 alone with `git diff HEAD~1 HEAD --stat` pointed at the 1B.3 commit hash. If reviewer surfaces issues, land follow-up commits — do NOT amend the historical 1B.3 commit. |
| 2026-05-26 | 2A.1 | Pre-commit hook failed first invocation: `.pre-commit-config.yaml` pinned `default_language_version: python: python3.13`, but the operator's machine runs python3.14. The pinned interpreter was unreachable, so virtualenv creation failed before any hook ran. Changed pin to `python3` (auto-discover the available Python 3 interpreter). CLAUDE.md specifies Python 3.13+ minimum; 3.14 satisfies the range. | If a future contributor lands on python3.13 specifically and wants reproducible builds, switch to `uv` / `python -m venv` pinning at project level (pyproject.toml `requires-python` already specifies the floor). Re-evaluate the pre-commit `default_language_version` pin once the Python version policy is locked in Phase 5 (lockfile work). |
| 2026-05-26 | 2B.1 | Plan called for a migration dropping `customers.shipment_volume_30d`, but grep confirms the column was never added — Phase 1's `0001_initial.py:62` carries a comment "No shipment_volume_30d column per operator amendment 2026-05-25; 30-day counts compute on demand from shipments." The work is already done. 2B.1 skipped; no migration written. | When executing 2B onward, the column simply does not exist. No action needed; the `customer_velocity_30d` Context field (from `count_user_30d` SQL) is the live replacement. If a future plan revives this scope, confirm via `\d customers` before writing a no-op migration. |
| 2026-06-02 | 5A.4 | Two pre-existing Docker infrastructure issues surfaced during the non-root user smoke test: (1) `python:3.13-slim` base lacks gcc; pytricia==1.3.0 is sdist-only on aarch64, so `pip install` failed — RESOLVED inline by adding `apt-get install build-essential` to Dockerfile; (2) `.env` sets `DATABASE_URL=localhost:5432` (host-side workflow), which docker-compose substitutes into the container env where it can't reach postgres. Smoke required shell-env override. NOT resolved in 5A.4; logged to BUGS.md. | 5D.2 already touches DATABASE_URL config and adds ALEMBIC_DATABASE_URL — natural place to split host vs container DATABASE_URL or add a `.env.docker`. Phase 6 multi-stage strips build-tools from the runtime image (must land before production deploy). Operator: at the 5A → 5B checkpoint, decide whether to expand 5D.2 to absorb the env-split or leave for Phase 6. |
| 2026-06-02 | 5D.2 | DATABASE_URL switched to riskd_app_login; 59 failed + 335 errored on first full-suite run. Root cause: RLS policies on 9 tables use `USING (tenant_id = current_setting('app.tenant_id')::int)` which also acts as WITH CHECK for INSERTs. Test fixtures (`seeded_tenant`, `_set_tenant_config` helpers, raw db_conn inserts in 30+ test files) do `INSERT INTO customers/users/shipments/decisions/...` without first calling `set_tenant_id` — they relied on the superuser bypass. Production endpoints (httpx integration tests) work fine because they call `set_tenant_id` inside the request transaction; the failures are in fixture-level raw db_conn manipulation. Stopped after first-attempt failure per autonomous-execution rule (5+ test threshold exceeded by an order of magnitude). | Operator decision: (a) refactor every test fixture and helper that touches a tenant-scoped table to wrap work in `async with conn.transaction(): set_tenant_id(conn, X); INSERT...` — this is substantial scope, 30+ files, but is the architecturally correct fix; OR (b) widen 5D.2 to add a pool-init callback that sets app.tenant_id session-scoped to a sentinel and refactor the fixtures incrementally — smaller but still significant; OR (c) revert 5D.2 entirely and defer RLS-active runtime to Phase 6. `.env` reverted to legacy `riskd:riskd` superuser for diagnostic runs. The repo-tracked 5D.2 work (migration 0008 already merged in d435008; canary refactor + alembic env.py split + .env.example + docker-compose.yml) stays staged but uncommitted pending direction. |
| 2026-06-03 | 6B.2 | PLAN_PHASE_6B.md 6B.2 estimated "~30 edited lines across 6 test files" for the test reconciliation; actual blast radius was 126 failures across 26 files. The plan's mechanical-per-file-edits approach was abandoned mid-execution in favor of a fixture-centric pivot: the four shared tenant fixtures in tests/conftest.py (`seeded_tenant`, `create_tenant_with_token`, `create_extra_tenant`, `seed_tenant_created_days_ago`) now seed `allowed_currencies = ["USD", "CAD"]` by default. This shifted the semantics of "the default tenant" in tests from "single-currency (whatever DEFAULT_ALLOWED_CURRENCIES is)" to "multi-currency". Per-test custom-config inserts in 8 integration files were also updated. Currency-aware tests in 4 files received semantic flips (USD-as-default-keyed assertion → CAD-as-default-keyed assertion). The pre-commit pytest-unit gate is restored; 1014/1014 passing. | Phase 6B.3 owns docs sweep (`.ai/decisions.md` Phase 4B amendment). Reviewers also surfaced (a) duplicated `'{"allowed_currencies": ["USD", "CAD"]}'::jsonb` JSONB literal across 7+ test sites — a D3 candidate for a shared helper constant in a future tidy-up commit; (b) the "stale USD-default tenant docstrings" sweep was completed inline in 6B.2 for test_currency_validation.py + test_currency_normalization_e2e.py. Future operator may extract the JSONB-literal duplication into a `_DEFAULT_TEST_TENANT_CONFIG_JSONB` module-level constant in tests/conftest.py. |
| 2026-06-03 | Phase 6 wrap | All five batches executed; 6E.3 commits this closeout row. Working tree clean on feat/refactor. 29 commits total; reviewer-panel discipline held throughout (no panel-skip events). | Operator: merge feat/refactor → main + tag v1.0.0 when ready per docs/production-launch-checklist.md. 5-month observation window starts at operator-driven launch. |
| 2026-06-08 | INFRA-CFN C1 | Plan §C1 specified `RecoveryWindowInDays: 30` on every `AWS::SecretsManager::Secret`. Verified via AWS docs: `RecoveryWindowInDays` is NOT a `Properties` field on `AWS::SecretsManager::Secret` — the recovery window is set at deletion time via the Secrets Manager API, with a 30-day default. CFN's `DeletionPolicy: Retain` plus the API-side default already give us the intended 30-day-recovery behavior. Adding the property to the template would fail cfn-lint. Plan text is incorrect on this point; behavior is correct. | When executing C4 (RDS DbMasterSecret) and any future secret resources, do NOT add `RecoveryWindowInDays` to the resource Properties. Rely on `DeletionPolicy: Retain` + the API's 30-day default. If operator wants an explicit non-default window (e.g., 7 or 14 days), it must be set via a separate `AWS::SecretsManager::SecretTargetAttachment`/post-deploy script, NOT the secret resource itself. Update plan §D14 and §C4 to drop the `RecoveryWindowInDays: 30` mention. |
| 2026-06-08 | INFRA-CFN C3 | Plan §C3 said `RdsSecurityGroup`: "ingress 5432 from EcsTaskSecurityGroup only; no egress". Implementation leaves RDS egress at the CFN default (allow-all) per AWS RDS guidance — restricting DB-instance egress can break backups, Performance Insights, and other AWS-managed services. ALB and ECS SGs also default-egress. Rationale recorded in the inline comment block at the top of the C3 resource group. Senior-engineer reviewer flagged plan-vs-code drift; reconciled here. | Future commits that touch SGs (none planned in C4-C8) should keep default egress on RDS. If operator hardens RDS egress later, must explicitly allowlist AWS service endpoints (S3 for backups, CloudWatch endpoints, etc.) or break those features. Update plan §C3 to drop "no egress" wording when next operator-driven plan edit happens. |
| 2026-06-08 | LOCAL-DEV / INFRA-CFN C5 | Cross-session staging collision. PLAN_LOCAL_DEV_TOOLING.md Commit 2 (Makefile + `.gitignore` `.tokens/` add) was staged in this worktree concurrently with the parallel INFRA-CFN C5 session. The CFN session's `git add` (broad scope) swept up the already-staged Makefile + `.gitignore`, so both deliverables landed inside commit 8ffdfe4 ("INFRA-CFN C5") instead of a dedicated `local-dev: Makefile…` commit. The functional content is correct: Makefile reviewed cycle 1→3 (senior=SHIP IT, security=LOW RISK/CLEAN, code-flow=CLEAN); see this conversation transcript. But INFRA-CFN C5's own reviewer panel did NOT review the Makefile (scope mismatch — they were looking at the ALB template). PLAN Commit 1 (`scripts/e2e_verify.py`) landed correctly as f28fa9e. | Operator: either (a) accept commit 8ffdfe4 as-is (work is correct + already reviewed; attribution is muddled in git log but never corrupting); or (b) when a follow-up tidy-up batch is run, append a marker commit whose body cross-references PLAN_LOCAL_DEV_TOOLING.md → 8ffdfe4 so future archaeologists find the right plan. Long-term: parallel sessions on a shared `.git/` should restrict `git add` to per-session file lists (avoid `git add -A` / `git add .`), or move to `git worktree` (already documented in CLAUDE.md "Parallel sessions and worktrees") for cleaner separation. |
